pipeline {
    agent docker
    
    stages {
        stage('Test') {
            steps {
             withDockerContainer(node:7-alpine) {
                sh 'node --version'
             }
            }
        }
    }
}
